1. set your virtual env to the one included (source opencv/bin/active).
2. run “python sliding_windows.py -i path/to/file” or “python sliding_windows.py -d path/to/folder” 


for blurs:
same arguments just run with blurs.py and press any key to shuffle through images. Comment out whichever blur u want to use